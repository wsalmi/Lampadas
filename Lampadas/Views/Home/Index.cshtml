@Html.Partial("_Home")
@section Scripts{
    <script src="~/Scripts/jquery.signalR-2.2.3.min.js"></script>
    <script src="@Url.Content("~/signalr/hubs")"></script>

    <script>
        var hubTest = $.connection.mainHub;
        var card = $('#TemplateCard').find('li#second-child').clone();

        hubTest.client.hello = function (nomeDoCara) {
            $('#DataHora').html(nomeDoCara);
        }

        hubTest.client.acender = function (lampada, seconds) { 
            $('#lampada-' + lampada).addClass('lampada-on');
            //glide.destroy();
            //$('#TemplateCard').append(card.html());
            //card.addClass("active");
            //card.find('#TituloCard').html('teste');
            //card.find('.segundos').html(seconds + 'ms');
            //glide.mount();
        }

        hubTest.client.apagar = function (lampada, seconds) {
            $('#lampada-' + lampada).removeClass('lampada-on');
            //glide.destroy();
            //$('#TemplateCard').html(card.html());
            //card.find('#TituloCard').html('teste');
            //card.find('.segundos').html(seconds + 'ms');
            //glide.mount();
        }

        $.connection.hub.start();

    </script>
}